query HeroGuidesInfo($heroId: Short) {
    heroStats {
        guide(heroId: $heroId) {
            heroId
            guides {
                steamAccountId
                matchId
            }
        }
    }
}